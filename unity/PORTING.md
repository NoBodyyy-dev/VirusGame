# Порт VirusGame: Godot 4.7 → Unity 6 (URP)

Перенос кооп-хакинг-роглайта с Godot (GDScript) на Unity (C#). Godot-оригинал
живёт в `../project/` и остаётся рабочим; этот каталог — отдельная ветка `unity-port`.

## Как открыть

1. Unity Hub → Add → выбрать `unity/VirusGame` (Unity 6000.0.x, см. `ProjectVersion.txt`).
2. При первом открытии Unity сам подтянет пакеты из `Packages/manifest.json`
   (URP, Input System, TextMeshPro) и сгенерирует `Library/`, `.csproj`, `.meta`.
3. Создать URP-ассет (Assets → Create → Rendering → URP Asset) и назначить его в
   Project Settings → Graphics (окружение/пост-обработка — см. TODO).
4. Сцены (`.unity`) в репозиторий не входят — их собирают в редакторе (ниже).

## Что портировано и ПРОВЕРЕНО компиляцией (.NET 8)

Движко-независимое **ядро правил** собрано `dotnet build` через шимы UnityEngine
(`_verify/`) — **0 ошибок**:

| Файл | Из Godot | Статус |
|---|---|---|
| `Core/GameData.cs` | таблицы game_state.gd (TIERS, ROOMS, SERVERS, цвета) | ✅ верифицировано |
| `Core/GameState.cs` | правила: кампания, зоны, флаги, Оракул, конфиг рейда | ✅ верифицировано |

## Что СОБРАНО и ЗАПУЩЕНО на Unity 6000.3.19f1

Проект открыт в реальном Unity, скомпилирован (0 ошибок) и **собран в
Standalone-плеер, который запускается с нуля исключений** (Player.log чист,
Direct3D 11, мир строится, игрок/камера спавнятся, HUD и текст рисуются).
Рендер — встроенный конвейер (без URP-ассета); материалы Mats работают и там.

| Файл | Из Godot | Примечание |
|---|---|---|
| `Core/GameStateBehaviour.cs` | автолоад GameState | синглтон + DontDestroyOnLoad |
| `Util/Mats.cs` | mats.gd | материалы через Standard-шейдер (RP-агностик), NoiseTexture→PerlinNoise |
| `Util/Build.cs` | хелперы _mesh_box/_collide/_label3d/_omni | GameObject+компоненты; текст на legacy `TextMesh` + шрифт ОС |
| `Util/Interactable.cs` | взаимодействие по дистанции + [E] | компонент + менеджер |
| `Player/VirusPlayer.cs` | player.gd | CharacterController (move_and_slide→Move) |
| `World/GridWorld.cs` | grid_world.gd (каркас) | комнаты, обучение, серверы, вход |
| `UI/Hud.cs` | grid_hud.gd | uGUI `UnityEngine.UI.Text` + шрифт ОС |
| `App/Boot.cs`, `App/SceneFlow.cs` | _ready / change_scene | бутстрап и переходы |
| `Net/NetStub.cs` | net.gd | ЗАГЛУШКА (см. ниже) |
| `Editor/UnityBuild.cs` | — | headless-сборка: сцена+Build Settings, always-included `Standard`, билд плеера |

### Как собрать/запустить из командной строки (batchmode, без GUI)
```
UNITY="C:/Program Files/Unity/Hub/Editor/6000.3.19f1/Editor/Unity.exe"
"$UNITY" -batchmode -quit -nographics -projectPath unity/VirusGame \
  -executeMethod Virus.EditorTools.UnityBuild.BuildWindows -logFile -
# → unity/Build/VirusUnity.exe
```

### Известные ограничения текущего среза (не баги сборки)
- Рендер на встроенном конвейере: нет SSAO/SSR/bloom/объёмного тумана (это URP).
- Материалы на Standard: без триплана и без реалистичных шумовых текстур Godot.
- Построен только каркас Грида + обучающий этап 0; вход в сервер грузит сцену
  `Level`, которой пока нет (см. стадии ниже).

## Обновление: портирована ВСЯ игровая петля (карта + сюжет)

Сцены: MainMenu (сюжет) → GridWorld → Level (рейд) → VictoryTunnel.
Грид: обучение (3 сервера, дверь), город (платформа+блоки), офисы (ярус,
лифт: 2 рычага+провод+роутер, падающие потолки, мох), бункер (3 генератора+
провода+рубильник, лазеры, блок-отключатель, паркур+кнопка+зип, красная
тревога 28/28), Оракул (15 головоломок, магистраль, рычаг, 3 территории,
4 стойки данных, ядро, 10 роботов, побег). Рейд: лут-физика, переноска,
портал/квота, фазы тревоги, ловушки-сферы, роботы-охотники, HP/баг,
эвакуация, результаты. PuzzleUI 4×4. Автозапуск сцен: -autogrid/-autoraid/
-autovictory. Все сцены проверены запуском: 0 исключений.

## Обновление 2: система эволюции + полный рейд

Портирована вся прогрессия из Godot (`game_state.gd`, `evolution_ui.gd`,
`virus_models.gd` + ловушки/крюки из `level.gd`):

- **Ядро** (`Core/GameData.cs`, `Core/GameState.cs`): классы 7 веток с
  пассивками/атрибутами, 11 активок, ветки по 5 умений, карьерные задания на
  глубину (deposits/tasks/raids/delivered), уровни УР.0–3 с ценами, откат
  умений, кража перепрошивкой (80/18/2%), возврат украденного после рейда,
  «сброс до нуля», апекс-расход BW ×1.5, evo-бонусы (скорость/BW/HP/кд),
  пассивки в StartHack (botnet BW 150 + двойной реген; ransomware +1 HP),
  **вспомогательный взлом** (заражённые серверы зоны снижают защиту, botnet
  удваивает), **комбо-серия вноса** (окно 20с, до ×1.5).
- **Дерево эволюции** (`Core/EvolutionTree.cs` — модель, `UI/EvolutionUI.cs` —
  полотно): созвездие как в референсе — градиент, звёзды, золотая арка, орбы
  со свечением и бегущей искрой, подписи веток по кругу, панель атрибутов,
  тултипы, зум колесом; [Tab] в Гриде, клик — прокачка, клик по взятому — откат.
- **Скины** (`Player/VirusModel.cs`): уникальный силуэт каждой ветки (бур червя,
  ящик трояна, замок ransomware, глаз spyware, попапы adware, капюшон rootkit,
  рой botnet), рост с уровнем, венец апекса на УР.3, эмблема доп. ветки,
  краб-«баг», ящик-морф; перестройка на лету (EvolutionChanged/сброс).
- **Рейд** (`World/Level.cs`): типизированные ловушки по тиру — лазер, клетка
  (8с), сброс (10с), притяжение к стене, метка (10с ведёт систему), патрон с
  перепрошивкой (−3 HP, замедление 15с, крадёт умение); роботы по углам, с 50%
  тревоги пускают **крюк-притягиватель** (летит 11.5 м/с, возвращается, робот
  замирает, жертва роняет груз и волочётся к роботу), на 100% — погоня;
  активки [Q]/[X]/[C] за Bandwidth с общим кд 8с−бонус; полевые задачи
  (консоли с удержанием E — карьера + тревога −6); анимация входа в сервер;
  HUD: BW-полоса, слоты активок, штамм.

## Обновление 3: LAN-кооп (v1 — общая кампания и стая)

`Net/NetStub` заменён рабочим коопом без внешних пакетов:

- **Ядро** (`Core/NetSync.cs`): текстовый протокол `TYPE|поле…\n`
  (HI/ID/SNAP/FLAG/NODE/POS/IDY/MSG/BYE), `NetFramer` режет TCP-чанки на
  строки, применение к GameState без эха; числа — в инвариантной культуре
  (русская локаль не ломает координаты). Покрыто SelfTest.
- **Транспорт** (`Net/NetManager.cs`): хост TcpListener:47777 + поток чтения
  на пира, ConcurrentQueue в главный поток; хост владеет кампанией и
  ретранслирует события. Клиент при входе получает снапшот (флаги, захваты,
  жар). Позиции 8 Гц; напарники видны **аватарами со скином их ветки** и
  именем — в Гриде и в рейде того же узла. Захваты серверов и флаги
  интерактива расшариваются живьём.
- **Меню**: «КООП: СОЗДАТЬ СТАЮ (LAN)» / «КООП: ВОЙТИ»; аргументы
  `-host` и `-join <ip>` для быстрых проверок двух инстансов.
- Ещё не синхронизировано (v2): внутрирейдовые сущности (лут/ловушки/роботы
  на стороне хоста), общий HP/тревога в рейде, совместная переноска.

## Обновление 4: Unity установлен, URP + пост-процессинг, смоук-раны

- На машине установлен Unity 6000.3.19f1 (Hub headless): сборка
  `-executeMethod Virus.EditorTools.UnityBuild.BuildWindows` → `unity/Build/VirusUnity.exe`.
- **URP 17.3** (Forward+ — много точечных огней, HDR, MSAA 4): пайплайн-ассеты
  создаёт `SetupURP()` в билд-скрипте; базовые материалы URP/Lit кладутся в
  `Assets/Resources` (тянут шейдер в билд без always-included взрыва вариантов).
  `Mats` строит материалы копией этих баз и ставит оба набора свойств
  (_BaseColor/_Color …) — работает и под URP, и под Standard.
- **PostFx** (`Util/PostFx.cs`): глобальный Volume — Bloom (неон светится),
  виньетка, ColorAdjustments, ACES-тонмаппинг; FXAA на камерах. Без URP
  тихо выключается.
- **Смоук-ран**: запуск `VirusUnity.exe -autoraid` (окно 1280×720 или
  fullscreen), скриншот экрана через System.Drawing, проверка
  `%USERPROFILE%\AppData\LocalLow\DefaultCompany\VirusGame\Player.log`
  на исключения. Рейд со всеми системами прошёл чисто (0 исключений).
- Геймплей-полировка: миникарта (лут/портал; роботы — spyware или охота),
  [F] бросок груза, тряска камеры, hit-флеш, колёса/радар роботов,
  тематические арены по тирам (дом/офис/дата-центр/бункер).

## Верификация без Unity (историческая — теперь Unity есть)

- `unity/_verify` — ядро правил + **SelfTest** (`dotnet run`): 30+ поведенческих
  проверок (ветки, уровни, задания, кража, комбо, вспомогательный взлом, дерево).
- `unity/_verify_full` — **весь** Unity-слой компилируется против расширенных
  шимов (`dotnet build`): ловит реальные ошибки компиляции без установленного
  Unity (сигнатуры совпадают с UnityEngine; поведение не эмулируется).

## Ещё НЕ портировано (стадии дальше)

Порядок по ценности/риску:

1. **Рейд `level.gd` (~3100 строк)** — ядро геймплея внутри сервера: СИСТЕМА,
   роботы-охотники, ловушки, лут-физика, полевые задачи, эвакуация. Самый большой
   отдельный кусок. → `World/Level*.cs` + `Loot.cs` (RigidBody) + `SystemDirector.cs`.
2. **Интерактив Грида целиком** — в срезе только каркас+обучение. Осталось:
   провода/генераторы/рычаги/лифты (AnimatableBody3D→кинематик Rigidbody),
   переносные блоки (карри+снап), лазерные ловушки, падающие потолки, зип-лайны,
   весь зал Оракула (пилоны, территории, стойки, ядро, побег).
3. **Головоломка `puzzle_ui.gd`** → `UI/PuzzleUI.cs` (uGUI-сетка 4×4).
4. **Туннель победы `victory_tunnel.gd`** → `World/VictoryTunnel.cs`.
5. **Дерево эволюции / меню / результаты** (evolution_ui, main_menu, results_ui,
   hud рейда) → uGUI/UI Toolkit.
6. **КООП `net.gd` — редизайн, не перевод.** Встроенного эквивалента
   высокоуровневого мультиплеера Godot в Unity нет. Взять **Netcode for GameObjects**
   (или Mirror/Fish-Net) и переложить модель «хост владеет состоянием»:
   `@rpc` → `[ServerRpc]/[ClientRpc]`, синки → `NetworkVariable`/`NetworkTransform`,
   `players`/`scores` → `NetworkList`. Хук `GameState.SendFlag` уже выведен наружу
   под это. Самая рискоёмкая стадия.
7. **Шейдеры** `.gdshader` (hologram/floor_grid/vignette) → Shader Graph/HLSL.
   Триплан материалов (в Godot из коробки) — тоже Shader Graph.
8. **Окружение**: WorldEnvironment (SSAO/SSR/glow/объёмный туман/тонемап) →
   URP Volume (Bloom/SSAO/Fog/Tonemapping). Сейчас в `BuildEnvironment` только
   базовый свет+ambient через RenderSettings.
9. **Аудио** `sfx.gd` → `AudioSource`-пул. **Частицы** (дождь/пыль) → VFX Graph.

## Сцены для Build Settings (создать в редакторе)

`MainMenu`, `GridWorld`, `Level`, `VictoryTunnel` — каждая с пустым GameObject и
компонентом-бутстрапом (`GridWorld` → `App/Boot.cs`). Имена должны совпадать с
константами в `App/SceneFlow.cs`.

## Проверка ядра локально

```
cd unity/_verify && dotnet build      # компилирует Core через шимы UnityEngine
```

`_verify/` и шимы в Unity не используются — это только офлайн-контроль логики.
